# M[container][type]
# 1 <= q <= 10      q: int the number of queries
# 1 <= n <= 100     n: int  the number of containers (rows) and ball types (columns).
# 0 <= M[container][type] <= 10**9


def organizingContainers(container):
